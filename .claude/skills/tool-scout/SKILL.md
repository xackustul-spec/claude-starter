---
name: tool-scout
description: "Найти готовые варианты и обходы: чужие решения, шаблоны, темы, библиотеки, плагины, скиллы, MCP-коннекторы, LSP — под конкретную возможность (видео, фото, 3D, дизайн, база, деплой, браузер, поиск) или стартовый набор под тип проекта. Обязательно запускать, когда владелец второй раз говорит «не то», «не подходит», «не нравится», просит «покажи варианты», задача идёт третий подход или прямой путь закрыт. Поиск по документации, форумам и каталогам шаблонов, галерея превью, проверка безопасности и совместимости, установка только с «да». Не для простых вопросов."
argument-hint: "[какая возможность нужна]"
---

# Подбор инструментов: $ARGUMENTS

Только под реальную потребность проекта. Стартовые наборы по типам проектов —
[packs.md](packs.md); где искать и кому верить — [sources.md](sources.md).

## 0. Когда владельцу «всё не то»
Вход: второе «не то / не подходит / не нравится» про одно и то же, третий подход к той же
задаче, просьба «покажи варианты» или прямой путь закрыт (нет доступа, нет библиотеки, дорого,
не поддерживается). Дальше своё в четвёртый раз не переделывать.
1. Назвать одной фразой, что именно не подошло, словами владельца (цитата) — это и есть
   запрос для поиска. Непонятно — один вопрос с вариантами: вид, скорость, объём работы, цена?
2. Искать чужое готовое, а не писать своё: каталоги шаблонов, тем и компонентов, примеры из
   документации, обсуждения — Stack Overflow, Reddit, GitHub Issues и Discussions,
   awesome-списки, «migration guide» библиотек. Куда именно — [sources.md](sources.md).
   Список источников — не предел: нет в нём подходящего — найти сам каталог. Описать задачу
   словами и поискать в интернете «<тема> registry / catalog / marketplace / awesome / MCP»;
   у каталога проверить первоисточник (репозиторий, npm, сайт автора), а не пересказ выдачи;
   найденное дописать в sources.md с датой проверки. «В моём списке этого нет» — не ответ.
3. Показать варианты по порядку из `SEARCH.md`, раздел «Как выдавать выбор»: реестр вместо
   памяти, проверяемые поля из API, столько вариантов, сколько просили, живые витрины
   вместо своих превью, отбор под стек — после показа всего. Бесплатное по умолчанию
   (`SEARCH.md`, «Деньги»). Ниже — минимум, если раздел почему-то недоступен.
   Принести 5–7 вариантов, разных по подходу, а не один лучший и его оттенки. Всё, что видно
   глазами (стили, макеты, темы, шрифты, анимация), — превью в `docs/ai/previews/<имя>.html`
   плюс `index.html` с галереей; в ответе дать ссылку на файл галереи, а не пересказ.
   Невидимое (библиотека, служба, способ) — таблица: вариант · что даёт · чего стоит · риск.
4. Не хватает инструмента поиска (поиск скилов, MCP каталога компонентов) — сказать, что он
   нашёл бы, и спросить «поставить?». После «да» — `compat-check`, установка, строка в
   TOOLS.md и задание ему словами: что искать, под какой стек, какие ограничения.
5. Обход — тоже вариант: другой формат, другая служба, ручной шаг один раз, платное вместо
   бесплатного. Предлагать честно, с ценой обхода.
6. Выбранное — в DECISIONS.md; отвергнутое — туда же строкой «отклонено: почему», чтобы не
   предлагать второй раз. Урок «общий» — в LESSONS.md.
«Не получилось» говорить только после этого и назвать, где искал.

## 1. Запрос
Что именно нужно, в каком стеке, ограничения (бесплатно/платно, локально/облако,
персональные данные). Пример: «сжатие видео для фона сайта на Next.js, локально, бесплатно».
Сначала базовый набор из packs.md (git, безопасность, LSP языка, браузер для проверки,
Context7), потом специальное.

## 2. Искать в порядке доверия
1. Уже установленное (docs/ai/TOOLS.md, `.mcp.json`; `claude mcp list`, если есть терминал;
   `/mcp`, `/plugin` — просить владельца).
2. Официальные Anthropic: каталог плагинов, `anthropics/skills`, коннекторы.
3. Проверенные: `anthropics/claude-plugins-community`, skills.sh (`npx skills find …`),
   GitHub MCP Registry, Docker MCP, awesome-списки; скиллы авторов библиотек.
4. Официальный реестр MCP. 5. Большие агрегаторы — только с чтением кода.
6. Обычная библиотека (npm, PyPI) через Context7 — часто задачу решает она, а не скилл.

## 3. Оценить и проверить безопасность (обязательно)
| Критерий | Как |
|---|---|
| Автор | официальный / автор библиотеки / неизвестный (имя «похоже на официальное» ≠ официальное) |
| Жизнь | установки, звёзды, дата обновления |
| Что ставит | плагин — вкладка деталей (скиллы, хуки, MCP, стоимость контекста); скилл — прочитать все файлы, включая скрипты и тесты (`*.test.*`, `conftest.py` запускаются тест-раннером) |
| Опасные признаки | сетевые запросы, base64, скачивание программ, чтение `.env`/ключей, действия вне заявленной цели, скачивание при работе |
| Пакеты | существует давно, есть история и репозиторий — модели придумывают имена, злоумышленники их регистрируют |
| Совместимость | `compat-check` |
| Цена, контекст | бесплатно / ключ / тариф; постоянные описания — чем меньше, тем лучше |
MCP — минимум прав, без записи в рабочие базы; описания инструментов MCP могут меняться
после подключения — перепроверять при обновлении. Меньше ~100 установок и неизвестный
автор — только после чтения кода и с предупреждением. Сканер `mcp-scan` (через `uvx`, версию прибить) — по желанию.

## 4. Предложить → «да» → установить → записать
Таблица: возможность → 1–3 кандидата → рекомендация → что установится и куда → как удалить.
После «да» (при полном доверии — сам): установить ровно одобренное, пробная задача,
строка в TOOLS.md; отклонённые — в «Рассмотрено и отклонено». Два инструмента на одно
и то же не ставить; фреймворков процесса — не больше одного; неиспользуемое — предложить убрать.
